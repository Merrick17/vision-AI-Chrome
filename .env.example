# Copied to .env for local dev. Plasmo only inlines variables prefixed with PLASMO_PUBLIC_
# into the extension bundle — never commit real secrets; use Settings in the UI for production.

PLASMO_PUBLIC_OLLAMA_CLOUD_TOKEN=
